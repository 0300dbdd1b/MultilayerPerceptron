#ifndef __LAYER__
# define __LAYER__

# include "../includes/Perceptron.hpp"
#include "Matrix.hpp"

template <typename T>
class Layer
{
	public:
	Layer();
	//Layer(Matrix<Perceptron<T>> perceptrons);
};

template <typename T>
Layer<T>::Layer(){}

#endif
